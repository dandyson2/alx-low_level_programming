#include "main.h"
/**
 * _sqrt_recursion - Your subconscious is looking for the dreamer
 * @n: input
 * Return: sqrt recursion
 */
int _sqrt_recursion(int n)
{
	int f = 0;

	if (f < 0)
		return (-1);
	else if
	{
		return (_evaluate(f, n));
	}
}
